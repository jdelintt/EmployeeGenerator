const Employee = require("./Employee");

class Intern extends Employee {
  constructor(school) {
    const school = this.school;
    getSchool();

    super(name, id, email);
    this.name = name;
    this.id = id;
    this.email = email;

    getRole () => {
        return "Intern";
  }
}
